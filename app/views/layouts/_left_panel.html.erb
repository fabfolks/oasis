<div class='col-md-4'>
  <div class='panel panel-info'>
    <div class='panel-heading'>
      <h3><%= link_to current_member.house.name, house_path(current_member.house)%></h3>
    </div>
    <div class='panel-body'>
      <ul class="list-group">
        <% @my_members.each do |member|%>
          <li class="list-group-item">
          <%= link_to member.name, member_path(member.id)%>
          </li>
        <% end %>
        <% if current_member.is_admin? %>
          <li class='list-group-item'>
          <h4><%= link_to 'Create new member', new_member_path %></h4>
          </li>
        <% end %>
          <li class='list-group-item'>
          <h4><%= link_to 'Search Members', members_path %></h4>
          </li>
      </ul>
    </div>
  </div>
</div>
</div>
