require 'spec_helper'

describe "Members" do
  describe "GET /members" do
    it "should return the list of members if member is logged in" do
      # Run the generator again with the --webrat flag if you want to use webrat methods/matchers
    end
  end
end
